[project]
name = "disputatio-bench"
version = "0.1.0"
description = "Head-to-head paper-review benchmark harness (refine.ink-faithful ruler + disputatio contestant driver)"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
disputatio-bench = "benchmark.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["benchmark"]
