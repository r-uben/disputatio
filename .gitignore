__pycache__/
*.egg-info/
dist/
build/
.env
*.pyc
.ruff_cache/
# workspace/ is NOT gitignored — agents (gemini CLI) treat gitignored paths as unreadable.
# Instead, just don't stage workspace files when committing.
