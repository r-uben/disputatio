__pycache__/
*.egg-info/
dist/
build/
.env
*.pyc
.ruff_cache/
TODO.md

# Runtime state from /disputatio and agent-ctl runs invoked at the repo root.
# Per-paper artifacts belong inside the Obsidian workspace, not here.
.claude/
# Symlink dir agy creates inside trusted workspaces (points to ~/.gemini config).
.antigravitycli/
# Subagent definitions are repo content, not runtime state — track them.
!.claude/agents/
!.claude/agents/**
_artifacts/
output/
# workspace/ is NOT gitignored — agents (gemini CLI) treat gitignored paths as unreadable.
# Instead, just don't stage workspace files when committing.

# Internal benchmark validation work — not for public repo
docs/benchmark/
docs/log/2026-04-29_forney-v7-1-validation.md

# Local outreach prep — sales playbook, recipient lists, etc. Not for public repo.
_outreach/

# Local bench corpus snapshot (paper PDFs, third-party reviews). Working-tree
# only — must never be tracked. Removed from the repo on 2026-05-11.
docs/archive/

# Vendored helpers are symlinks to their canonical homes, NOT committed
# snapshots: codex/gemini/agent_ctl.py all live in ~/.config/ai-skills/ (a
# private git repo). The symlinks resolve to maintainer-local absolute paths
# and would dangle for anyone else, so they must never be tracked or pushed.
# Cloners: run `./install.sh` (links the disputatio skill only) and bring your
# own codex/gemini/agent_ctl.py. (vendor/agy-set-model + vendor/README.md stay
# tracked — agy-set-model has no home outside this repo.)
vendor/agent_ctl.py
vendor/skills/codex
vendor/skills/gemini

# macOS noise
.DS_Store
.venv/
*.egg-info/
