[project]
name = "disputatio-compare"
version = "0.1.0"
description = "Evaluation harness: disputatio vs coarse.ink"
requires-python = ">=3.11"
dependencies = [
    "litellm>=1.60",
    "pydantic>=2.0",
]

[project.scripts]
judge = "judge:main"
adapt = "adapt:main"
